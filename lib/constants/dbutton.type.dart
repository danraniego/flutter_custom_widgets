enum DButtonType {
  elevated,
  outlined
}

enum DButtonColor {
  primary,
  danger,
}

enum DButtonSize {
  small,
  medium,
  large,
}

enum DButtonContent {
  text,
  icon
}

enum DButtonShape {
  rounded,
  square,
  circle,
}